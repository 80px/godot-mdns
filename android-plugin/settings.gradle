pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

// The project name determines the output AAR filename:
//   build/outputs/aar/MulticastLockPlugin-release.aar
rootProject.name = 'MulticastLockPlugin'
